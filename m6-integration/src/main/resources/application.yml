spring:
  application:
    name: m6-integration
  rabbitmq:
    host: localhost
    port: 5672

atech:
  tarifas:
    url: http://localhost:${server.port:8080}/tarifas

logging:
  level:
    org.springframework.integration: info
